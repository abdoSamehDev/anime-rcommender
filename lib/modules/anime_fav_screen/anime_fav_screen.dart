import 'package:flutter/material.dart';

class AnimeFavScreen extends StatelessWidget {
  const AnimeFavScreen({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const Center( child: Text('Favourites'));
  }
}
